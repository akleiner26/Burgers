<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devour}}Reorder!{{else}} Devour Your Burger!{{/if}}
    </button>

    <button class="del-btn" data-id="{{id}}">
        Toss Burger
    </button>
</li>