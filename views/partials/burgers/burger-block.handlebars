<li>
    {{burger_name}}

{{#if devour}}
    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
       Eat <i class="fa fa-cutlery" aria-hidden="true"></i>
    </button>
{{/if}}
    <button class="del-btn" data-id="{{id}}">
     Toss <i class="fa fa-trash" aria-hidden="true"></i>
    </button>
</li>